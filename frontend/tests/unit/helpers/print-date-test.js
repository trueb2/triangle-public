
import { printDate } from 'frontend/helpers/print-date';
import { module, test } from 'qunit';

module('Unit | Helper | print date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = printDate([42]);
  assert.ok(result);
});

