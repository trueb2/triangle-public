
import { initials } from 'frontend/helpers/initials';
import { module, test } from 'qunit';

module('Unit | Helper | initials');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = initials([42]);
  assert.ok(result);
});

